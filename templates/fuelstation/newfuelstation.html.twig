{# empty Twig template #}
{% extends "base.html.twig" %}

{% block content %}
    <div class="row">
        <div class="col-lg-2">
        </div>
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header d-flex align-content-center">
                    <h3 class="h4">Register New Fuel Station</h3>
                </div>
                <div class="card-body">
                    {% for flash_message in app.flashes('registrationsuccess') %}
                        <div class="alert alert-success" role="alert">
                            {{ flash_message }}
                        </div>
                    {% endfor %}
                    {{form(form, {'attr': {'novalidate': 'novalidate'}})}}
                </div>
            </div>
        </div>
        <div class="col-lg-2">
        </div>

    </div>
{% endblock content %}
